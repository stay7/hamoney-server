server:
  tomcat:
    accesslog:
      enabled: true
spring:
  datasource:
    username: sa
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: true